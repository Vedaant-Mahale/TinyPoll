{
  "name": "tinypoll",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build",
    "start": "cd server && node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^4.19.0",
    "pg": "^8.16.3"
  }
}
