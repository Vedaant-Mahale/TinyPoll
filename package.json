{
  "name": "tinypoll",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build",
    "start": "cd server && node server.js"
  },
  "dependencies": {
    "express": "^4.19.0",
    "pg": "^8.16.3"
  }
}
